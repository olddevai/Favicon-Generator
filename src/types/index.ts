export interface IconPreview {
  size: string;
  url: string;
}

export interface ManifestData {
  name: string;
  shortName: string;
  description: string;
  themeColor: string;
  backgroundColor: string;
}